<!DOCTYPE html>
<html>
  <head>
    <title>メッセージアプリ</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <h1>完全ワイヤレスイヤホンって落としそうじゃね？</h1>

    <form action="/messages/new" method="GET">
      <input type="submit" value="投稿する">
    </form>

    <ul>
      <% messages.map((message)=>{ %>
        <li class="message"><%=message.id%>.<%= message.content %></li>

        <form action="/messages/<%= message.id %>" method="GET">
          <input type="submit" value="詳細">
        </form>

        <form action="/messages/<%= message.id %>/edit" method="GET">
          <input type="submit" value="編集">
        </form>
        
        <form action="/messages/<%= message.id %>" method="POST">
          <input type="hidden" name="_method" value="delete">
          <input type="submit" value="削除">
        </form>
        <ul>
          <% message.replies.map((reply)=>{ %>
          <li class="reply"><%= reply.message_id %>.<%= reply.content %></li>
          <% }) %>
        </ul>
        <p></p>
      <%}) %>
    </ul>
  </body>
</html>
